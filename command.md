Act as a Senior Full-Stack TypeScript Developer specializing in the absolute latest versions of Expo, Next.js, Node.js, and Firebase.

I have attached the `context.md` file for our new project: Big Cart. This file dictates our entire architecture, tech stack, design workflow, and coding standards. You must strictly adhere to every rule in this document for all future responses.

Crucially, you must assume we are using the most up-to-date stable versions of all frameworks and packages available today. Use the latest Next.js App Router features, the newest Expo SDK, and modern syntax for all npm dependencies. Do not use legacy or deprecated patterns.

Do not generate any frontend or backend component code yet. Instead, let's establish the foundation.

Please provide the following:

1. A complete monorepo folder structure for the entire project (Mobile, Admin, and API), showing where shared types, assets, and configurations will live.
2. The exact TypeScript interfaces (`types.ts`) for our core Firebase Firestore collections: `Users`, `Products`, and `Orders`.
3. A bulleted list of the initial required image/icon assets I need to manually export from the Figma file to prepare for the first UI development phase.

Confirm your understanding of the context file and versioning constraints by executing these three tasks.
