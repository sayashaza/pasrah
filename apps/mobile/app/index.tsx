import { Redirect } from 'expo-router';

export default function Index() {
  // In the future, check auth state here. Currently, routexport default function Index() {
  return <Redirect href="/(tabs)" />;
}
